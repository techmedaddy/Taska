{
  "compilerOptions": {
    /* Basic Options */
    "target": "ESNext",                            /* Use latest JavaScript features for compatibility. */
    "module": "CommonJS",                          /* Ensures compatibility with Node.js. */
    "moduleResolution": "node",                    /* Specifies how TypeScript looks up a file from a given module specifier. */
    "outDir": "./dist",                            /* Specify an output folder for all emitted files. */
    "rootDir": "./src",                            /* Ensure compilation starts from src/. */
    "strict": true,                                /* Enable all strict type-checking options. */

    /* Decorators and Metadata */
    "experimentalDecorators": true,               /* Enable support for legacy experimental decorators. */
    "emitDecoratorMetadata": true,                /* Emit design-type metadata for decorated declarations in source files. */

    /* Interop and Compatibility */
    "esModuleInterop": true,                      /* Fixes issues with CommonJS module imports like 'express-validator'. */
    "allowSyntheticDefaultImports": true,         /* Allows importing CommonJS modules with default exports. */
    "forceConsistentCasingInFileNames": true,     /* Ensure that casing is correct in imports. */
    "skipLibCheck": true,                         /* Skip type checking of declaration files (.d.ts). */

    /* Source Maps */
    "sourceMap": true,                            /* Create source map files for debugging. */
    "inlineSources": true,                        /* Include source code in the sourcemaps inside the emitted JavaScript. */

    /* Paths and Types */
    "baseUrl": "./",                              /* Specify the base directory to resolve non-relative module names. */
    "paths": {                                    /* Map module paths for cleaner imports (adjust as needed). */
      "@src/*": ["src/*"],
      "@models/*": ["src/models/*"],
      "@controllers/*": ["src/controllers/*"],
      "@services/*": ["src/services/*"],
      "@routes/*": ["src/routes/*"],
      "@utils/*": ["src/utils/*"]
    },
    "typeRoots": ["node_modules/@types"],         /* Specify multiple folders that act like './node_modules/@types'. */

    /* Additional Options */
    "allowJs": false,                             /* Disallow JavaScript files in the project. */
    "resolveJsonModule": true                     /* Enable importing .json files. */
  },
  "include": ["src"],                             /* Specify the files or patterns to include in the compilation. */
  "exclude": ["node_modules", "dist"]             /* Exclude compiled output and dependencies from compilation. */
}
